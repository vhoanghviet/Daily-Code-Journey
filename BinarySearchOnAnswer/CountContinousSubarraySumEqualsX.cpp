#include <bits/stdc++.h>
using ll = long long;
using namespace std;

// Do bài này sử dụng map -> mình sẽ skip và học lại map trước khi làm

void solve()
{
    int n, x;
    cin >> n >> x;
    vector<int> a(n);
    for (int i = 0; i < n; i++)
    {
        cin >> a[i];
    }
    ll tong = 0;
    map<ll, int> mp;
    for(int i = 0; i < n; i++) {

    }
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    solve();
    return 0;
}